rsync -av --delete _site/ titan.djones.co:servers/portfolio.djones.co/resume/
docker run --rm \
  -u $(id -u):$(id -g) \
  -e HOME \
  -v $HOME:$HOME \
  -v $(pwd):/host \
  -w /host \
  tortxof/awscli \
  aws s3 sync _site s3://portfolio.djones.co/resume --delete
